# Project Instructions

## Python Environment

- 本项目统一使用 Miniconda 的 `jax` 环境。运行 Python、安装依赖、执行测试或启动训练时，必须使用 `C:\Users\Hollon\miniconda3\envs\jax\python.exe`，或先通过 `conda activate jax` 激活该环境；不要使用 Miniconda `base`、系统 Python 或其他 Conda 环境。
